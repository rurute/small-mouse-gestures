# プライバシーポリシー

対象: Chrome 拡張機能「small mouse gestures」
最終更新: 2026-08-20

## 要約

この拡張機能は、利用者の情報を収集しません。開発者はいかなるデータも受け取りません。
外部のサーバーへの通信を一切行いません。

## この拡張機能が読み取るもの

マウスジェスチャを認識するために、ウェブページ上での次の情報を読み取ります。

- マウスのボタンの押下状態（左・右・中）
- マウスカーソルの座標
- Esc キーが押されたこと（ジェスチャのキャンセルに使用）

これらは、ジェスチャの方向を判定するためにお使いの端末内でのみ使用します。判定が
終わった時点で破棄され、保存も送信もしません。

## この拡張機能が読み取らないもの

すべてのウェブサイトで動作する必要があるため、この拡張機能は全サイトへのアクセス権を
要求します。しかし、次の情報は読み取りません。

- 閲覧履歴
- 開いているページの URL、タイトル
- ページの内容、テキスト、画像
- 入力フォームの内容、パスワード、Cookie
- 個人を識別できる情報

この拡張機能は Chrome の `tabs` 権限を要求していません。そのため、タブの URL や
タイトルにアクセスする手段をそもそも持っていません。

## 保存するもの

利用者が設定した内容だけを保存します。

- ジェスチャとアクションの割り当て
- ジェスチャと判定する移動距離などのしきい値
- 軌跡の表示設定（表示の有無、色、太さ）

保存先は Chrome の設定ストレージ（`chrome.storage.sync`）です。**Chrome の同期機能を
有効にしている場合、この設定は Chrome 自身の仕組みによってお使いの Google アカウント
を通じて他の端末と同期されます。**これは Chrome の機能であり、この拡張機能が独自の
サーバーへ送信しているものではありません。同期を望まない場合は、Chrome の同期設定から
拡張機能の設定を同期対象外にできます。

保存されるのは上記の設定値のみで、閲覧に関する情報は一切含まれません。

## 外部への通信

行いません。この拡張機能には、ネットワーク通信を行うコードが存在しません
（`fetch`、`XMLHttpRequest`、`WebSocket`、`sendBeacon` のいずれも使用していません）。

アクセス解析、クラッシュレポート、広告、トラッキングのいずれも組み込んでいません。

## 第三者への提供

提供する情報がありません。開発者を含め、誰にもデータが渡ることはありません。

## ソースコード

この拡張機能のソースコードは公開されています。難読化、minify、バンドルのいずれも
行っていないため、上記の内容はコードを読んで確認できます。

https://github.com/rurute/small-mouse-gestures

## ポリシーの変更

この文書を変更した場合は、上記リポジトリの更新履歴に残ります。データの取り扱いに
関わる変更を行う場合は、拡張機能の更新時に利用者へ明示します。

## お問い合わせ

上記リポジトリの Issues からご連絡ください。

https://github.com/rurute/small-mouse-gestures/issues
